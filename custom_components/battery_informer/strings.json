{
  "config": {
    "step": {
      "user": {
        "title": "Battery Informer",
        "description": "Configure battery threshold notifications.",
        "data": {
          "warning_threshold": "Warning threshold (%)",
          "critical_threshold": "Critical threshold (%)",
          "notify_service": "Notify service",
          "rescan_interval_minutes": "Full rescan interval (minutes)",
          "monitoring_mode": "Monitoring mode",
          "warning_template": "Warning message template",
          "critical_template": "Critical message template",
          "recovery_template": "Recovery message template"
        }
      }
    },
    "error": {
      "invalid_thresholds": "Critical threshold must be lower than warning threshold",
      "notify_service_not_found": "The selected notify service was not found",
      "invalid_notify_service": "Invalid notify service format"
    },
    "abort": {
      "already_configured": "Battery Informer is already configured"
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Battery Informer options",
        "data": {
          "warning_threshold": "Warning threshold (%)",
          "critical_threshold": "Critical threshold (%)",
          "notify_service": "Notify service",
          "rescan_interval_minutes": "Full rescan interval (minutes)",
          "monitoring_mode": "Monitoring mode",
          "excluded_entities": "Excluded battery entities",
          "included_entities": "Included battery entities",
          "warning_template": "Warning message template",
          "critical_template": "Critical message template",
          "recovery_template": "Recovery message template",
          "reset_templates_to_default": "Reset templates to default"
        }
      }
    }
  }
}
